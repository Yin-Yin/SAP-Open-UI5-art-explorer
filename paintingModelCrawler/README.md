#_Paintings Model Crawler_

This is an work in progress. It is nowhere near finished and not working at all. 

first run
```sh
    npm install 
```
 
to install the node modules

to start the app
 ```sh
    node app.js 
 ```